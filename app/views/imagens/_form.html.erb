<%= simple_form_for(@imagem) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :imagem, as: :file %><br/>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>

<% end %>
